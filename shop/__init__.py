# The __init__.py file marks the shop directory as a Python package,
# allowing Django and Python to discover and import the application's
# modules correctly. Although no initialisation code is currently
# required, this file provides a location for package-level
# configuration should the Shop application require shared startup
# behaviour in the future.
