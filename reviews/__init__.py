# The __init__.py file identifies the reviews directory as a Python
# package, allowing Django to discover and import its modules correctly.
# Although the file is currently empty, it provides a location for
# package-level initialisation code should the Reviews application
# require shared configuration or startup behaviour in the future.
