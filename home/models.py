# The models.py file defines the database models used by the Home
# application. Each Django model represents a database table, allowing
# Django's Object-Relational Mapper (ORM) to manage data through Python
# objects rather than raw SQL queries. Although the Home application
# does not currently require database models, this file provides a
# dedicated location for implementing persistent data structures should
# future enhancements require them.
